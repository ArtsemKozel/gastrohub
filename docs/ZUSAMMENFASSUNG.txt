╔══════════════════════════════════════════════════════════════════════════════╗
║                         GASTROHUB — ZUSAMMENFASSUNG                        ║
║                              Stand: 25.04.2026                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 WAS IST GASTROHUB?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GastroHub ist eine Neuentwicklung auf Basis von PlanIt. PlanIt war eine
monolithische Single-Page-App (eine große admin.js + employee.js). GastroHub
zerlegt diese Logik in eine modulare Ordnerstruktur mit getrennten JS-Dateien
pro Feature-Bereich und drei klar getrennten Nutzergruppen.

Quell-Repository:  /Users/artchiebald/Documents/PlanIt/
Ziel-Repository:   /Users/artchiebald/Documents/gastrohub-new/
                   https://github.com/ArtsemKozel/gastrohub

Datenbank:         Supabase (identischer Account und URL wie PlanIt)
                   URL: https://ulazvcrpasnhufzlywxf.supabase.co

Deployment:        Cloudflare Pages
                   Domain: gastro-hub.org
                   Branch: main → automatischer Deploy bei Push


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ORDNERSTRUKTUR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  gastrohub-new/
  ├── app/                    ← App-Code (admin, employee, pos)
  │   ├── admin.html
  │   ├── employee.html
  │   ├── pos.html
  │   ├── index.html
  │   ├── register.html
  │   ├── js/
  │   │   ├── shared/         db.js, auth.js, utils.js
  │   │   ├── admin/          init.js, shifts.js, team.js, vacation.js,
  │   │   │                   hours.js, tips.js, inventory.js, hygiene.js,
  │   │   │                   tasks.js, terminations.js, margincalc.js,
  │   │   │                   payroll.js, restaurant.js, time_entries.js,
  │   │   │                   temperature.js, notify.js
  │   │   ├── employee/       init.js, overview.js, vacation.js,
  │   │   │                   availability.js, swaps.js, inventory.js,
  │   │   │                   profil.js, schichtplan.js, stunden.js,
  │   │   │                   trinkgeld.js, payroll.js, temperature.js
  │   │   └── pos/            clock.js, breaks.js
  │   └── css/                (noch leer — Styles inline in HTML)
  ├── web/                    ← Marketing-/Landingpage
  └── docs/                   ← Diese Dokumentation


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 DATENBANK-TABELLEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Von PlanIt übernommen (teils erweitert):
    employees_planit      Mitarbeiterstammdaten
                          Original: id, name, department, password_hash,
                          login_code, user_id, is_active
                          Neu hinzugefügt:
                            can_do_inventory    boolean — Inventur-Delegation
                            can_do_temperature  boolean — Temperaturkontrolle-
                                                Delegation (wie can_do_inventory)
                            employment_type     text — Beschäftigungsart
                            hourly_rate         numeric — Stundenlohn
                            wage_type           text — Lohnart (Stunden/Monat)

    planit_restaurants    Restaurant-Konten (id, name, owner_id)
    shifts                Schichtplanung
    shift_templates       Schichtvorlagen
    vacation_requests     Urlaubsanträge
    availability          Verfügbarkeitsangaben
    tips                  Trinkgeldverteilung
    inventory             Inventureinträge
    hygiene_logs          Hygieneprotokoll
    tasks / notes         Aufgaben und Notizen

  Neu in GastroHub:
    gh_time_entries       Stempelzeiten der Mitarbeiter
                          Spalten: id, user_id, employee_id,
                          clock_in (timestamptz), clock_out (timestamptz),
                          note (text),
                          is_manual (boolean) — true wenn manuell vom Admin
                          nachgetragen (zeigt "manuell"-Badge im Clock-Block)
                          Hinweis: RLS auf anon key blockiert chained
                          .insert().select() — nach INSERT immer separates
                          SELECT durchführen

    gh_breaks             Pausen zu einem Stempelzeit-Eintrag
                          Spalten: id, user_id, employee_id,
                          time_entry_id (FK → gh_time_entries.id),
                          break_start (timestamptz), break_end (timestamptz)
                          Hinweis: Beim Löschen eines gh_time_entries-Eintrags
                          zuerst alle gh_breaks mit time_entry_id löschen

    planit_payrolls       Vorlohnabrechnungen (Lohnzettel-Entwürfe)
                          Spalten: id, user_id, employee_id, month (YYYY-MM),
                          data (jsonb mit Stunden, Zuschlägen, Abzügen),
                          created_at

    planit_terminations   Kündigungsanträge und Arbeitgeber-Kündigungen
                          Spalten: id, user_id, employee_id, requested_date,
                          approved_date, reason, status (pending/approved/
                          rejected), initiated_by (employer/employee), pdf_url

    employment_phases     Beschäftigungsphasen eines Mitarbeiters
                          Spalten: id, employee_id, start_date, end_date
                          end_date = null → aktuell aktive Phase

    temperature_devices   Temperaturkontrolle-Geräte/Messpunkte
                          Spalten: id, user_id, name, min_temp (numeric),
                          max_temp (numeric), created_at
                          Sortierung: immer nach created_at ascending

    temperature_logs      Temperaturmesswerte
                          Spalten: id, user_id, employee_id, device_id
                          (FK → temperature_devices.id), temperature (numeric),
                          note (text), log_date (date), created_at


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 DREI NUTZERGRUPPEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1. ADMIN       → app/admin.html   Login via Supabase Auth (Email + Passwort)
                                    Zugriff auf alle Verwaltungsfunktionen
                                    Session: Supabase JWT

  2. MITARBEITER → app/employee.html  Login via Kürzel + PIN (localStorage)
                                    Zugriff auf persönliche Schichten, Urlaub etc.
                                    Session: localStorage-Key "planit_employee"
                                    OneSignal: login(String(employee.id)) beim
                                    App-Start für Push-Benachrichtigungen

  3. POS         → app/pos.html    Kiosk-Stempeluhr für Mitarbeiter
                                    Kein Login — user_id aus URL-Parameter ?uid=
                                    Fallback: 7bb9b579-edd9-449a-840c-21fb33bde772


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PUSH-BENACHRICHTIGUNGEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Infrastruktur: OneSignal SDK (Mitarbeiter-App) + Cloudflare Worker als
  Relay (gastrohub-notify.artsem86.workers.dev)

  Funktion sendPushNotification(title, message, external_id?) in notify.js
  sendet einen POST an den Worker. external_id ist die employee.id als String
  (OneSignal external_id) — bei Aufruf ohne external_id wird an alle gesendet.

  Ausgelöste Benachrichtigungen:
    Schichtplan veröffentlicht   → an alle Mitarbeiter (kein external_id)
    Offene Schicht zugewiesen    → an den zugewiesenen Mitarbeiter
    Andere Bewerber abgelehnt    → an alle anderen Bewerber derselben Schicht
    Urlaubsantrag genehmigt      → an den Mitarbeiter
    Urlaubsantrag abgelehnt      → an den Mitarbeiter
    Kündigung genehmigt          → an den Mitarbeiter
    Kündigung abgelehnt          → an den Mitarbeiter


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 DESIGN-ENTSCHEIDUNGEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Font:          Inter (Google Fonts)
  Hintergrund:   #F5F0EB  (warmes Off-White, CSS-Variable --color-bg)
  Primärfarbe:   #B28A6E  (Terrakotta/Braun) — Buttons, Fokus, Akzente
  Dunkelbraun:   #8B6F47  — Icons, sekundäre Buttons
  Grün:          #6B8E6F  — Einstempeln, positive Aktionen
  Amber:         #F59E0B  — Pause-Zustand
  Fehler:        #E05555

  POS-Kiosk orientiert sich an ShiftIn (externes Referenz-Projekt) —
  inline CSS in renderPinScreen() und renderEmployeeScreen() in clock.js.
  Admin-Bereich verwendet CSS-Variablen aus admin.html <style>-Block.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 AKTUELLER STAND (25.04.2026)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  FERTIG ✓
  ─────────────────────────────────────────────────────────────
  index.html              Login-Seite (Admin + Mitarbeiter, Pill-Tabs)
  register.html           Registrierung (GastroHub-Design, planit_restaurants)
  admin.html              HTML-Struktur vollständig, alle Modals vorhanden
  employee.html           HTML-Struktur vollständig

  js/shared/db.js         Supabase-Client-Initialisierung (global "db")
  js/shared/auth.js       Login, Session-Checks, Logout für alle Gruppen
  js/shared/utils.js      Feiertage BW, Datum-Formatierung, Abteilungen

  js/admin/init.js        requireAdminSession(), DOMContentLoaded, switchTab()
  js/admin/shifts.js      Schichtplanung vollständig:
                          — Schicht erstellen / bearbeiten / löschen
                          — Wiederholung (N Wochen)
                          — Vorlagen laden und anwenden
                          — Verfügbarkeits-Warnungen
                          — Tatsächliche Zeiten (klappbarer Block)
                          — Stempelzeiten aus gh_time_entries (klappbarer Block)
                            mit Netto-Dauer, Pausen abgezogen, Gesamt-Netto
                          — Kommentar (note) aus gh_time_entries im Block
                          — Eintrag löschen (zuerst gh_breaks, dann Eintrag)
                          — Stempelzeiten manuell nachtragen (is_manual-Flag,
                            "manuell"-Badge über der Eintragszeile)
                          — Überschneidungs-Prüfung beim Schicht-Speichern
                            (Intervall-Overlap-Check, Bestätigung via confirm())
                          — ⋮-Zelle im Wochengitter: Extra Schicht,
                            Selektiv löschen, Alle löschen
                          — Multi-Schicht-Badge (Anzahl-Bubble oben rechts
                            wenn > 1 Schicht am selben Tag)
                          — Schichtplan veröffentlichen (Push-Benachrichtigung)
  js/admin/team.js        Team-Verwaltung
  js/admin/vacation.js    Urlaubsverwaltung
                          — Push-Benachrichtigungen bei Genehmigung/Ablehnung
                          — Offene Schicht zuweisen mit Benachrichtigung
  js/admin/hours.js       Stundenübersicht
  js/admin/tips.js        Trinkgeldverteilung
  js/admin/inventory.js   Inventur
  js/admin/hygiene.js     Hygieneprotokoll
  js/admin/tasks.js       Aufgaben & Notizen
  js/admin/terminations.js  Abgänge
                          — Push-Benachrichtigungen bei Genehmigung/Ablehnung
  js/admin/margincalc.js  Deckungsbeitragsrechnung (MarginCalc):
                          Umsatz, Wareneinsatz, Personalkosten, DB I / DB II
  js/admin/payroll.js     Vorlohnabrechnung:
                          Lohnzettel-Entwurf pro Mitarbeiter und Monat,
                          Speichern in planit_payrolls, PDF-Export via jsPDF
  js/admin/restaurant.js  Restaurant-Einstellungen
  js/admin/time_entries.js  Stempelzeiten-Übersicht (alle Mitarbeiter)
  js/admin/temperature.js Temperaturkontrolle:
                          — Tagesansicht mit klappbaren Tageskarten
                          — Geräteverwaltung (Anlegen, Bearbeiten, Löschen)
                          — Delegation an Mitarbeiter (can_do_temperature)
                          — PDF-Export mit Von/Bis-Datumsbereich
  js/admin/notify.js      Push-Benachrichtigungen:
                          sendPushNotification(title, message, external_id?)
                          POST an Cloudflare Worker

  js/employee/init.js     requireEmployeeSession(), DOMContentLoaded, switchTab()
                          OneSignal.login(String(currentEmployee.id)) beim Start
  js/employee/overview.js Übersicht (Schichtplan der Woche)
                          checkTemperatureVisibility() — zeigt/versteckt
                          Temperaturkontrolle-Menüpunkt je nach Flag
  js/employee/vacation.js Urlaubsantrag & Kalender
  js/employee/availability.js  Verfügbarkeitsangaben
  js/employee/swaps.js    Schichttausch
  js/employee/inventory.js     Inventurformular (Mitarbeiter)
  js/employee/profil.js   Profilseite
  js/employee/schichtplan.js   Schichtplan-Ansicht (Mitarbeiter)
  js/employee/stunden.js  Stundenübersicht (Mitarbeiter)
  js/employee/trinkgeld.js     Trinkgeld-Ansicht (Mitarbeiter)
  js/employee/payroll.js  Lohnzettel-Ansicht (Mitarbeiter)
  js/employee/temperature.js   Temperaturkontrolle (Mitarbeiter):
                          — Tagesansicht mit Eingabefeld pro Gerät
                          — Nur sichtbar wenn can_do_temperature = true

  pos.html                POS-Kiosk vollständig (Shell + Styles)
  js/pos/clock.js         PIN-Login, Ein-/Ausstempeln, Kommentar, Logout
                          — Zeitanzeige: laufende Arbeitszeit und Pausenzeit
                            im Employee-Screen (wird sekündlich aktualisiert)
  js/pos/breaks.js        Pause starten / beenden (gh_breaks)

  OFFEN ✗
  ─────────────────────────────────────────────────────────────
  js/pos/pos.js           POS-Hauptlogik (Kassensystem, noch nicht begonnen)

  CSS (alle leer):
  css/base.css, css/components.css, css/admin.css, css/employee.css, css/pos.css


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ARBEITSREGELN FÜR CLAUDE CODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Diese Regeln gelten für alle Aufträge an Claude Code in diesem Projekt und
sollen eine konsistente, nachvollziehbare Zusammenarbeit sicherstellen.

Aufträge werden als zusammenhängender Fließtext auf Deutsch formuliert —
keine Aufzählungslisten, keine Codeblöcke im Auftragstext. Der Auftragstext
nennt immer den Dateinamen und den Funktionsnamen, der geändert werden soll,
damit Claude Code ohne Rückfragen an die richtige Stelle navigieren kann.

Pro Auftrag wird genau eine inhaltliche Änderung beschrieben. Mehrere
unabhängige Änderungen werden als separate Aufträge gestellt, nicht
zusammengefasst, um Verwechslungen beim Commit-Text zu vermeiden und die
Git-Historie übersichtlich zu halten.

Mobile und Desktop werden immer gleichzeitig berücksichtigt. Wann immer eine
UI-Änderung in einer Ansicht gemacht wird, muss geprüft werden ob dieselbe
Änderung auch für die andere Bildschirmgröße passt oder angepasst werden muss.

Nach jedem abgeschlossenen Feature oder jeder Änderung wird der Stand sofort
committed und zu GitHub gepusht. Es wird nicht auf mehrere fertige Aufgaben
gewartet — jede Änderung erhält ihren eigenen Commit mit aussagekräftigem
Commit-Text auf Deutsch.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PRE-COMMIT-HOOK (Cache-Busting)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Ein Pre-Commit-Hook aktualisiert automatisch die ?v=-Query-Parameter aller
<script src="...?v=...">-Tags in den HTML-Dateien (admin.html, employee.html,
pos.html). Dadurch werden Browser-Caches bei jedem Deploy zuverlässig
invalidiert ohne manuellen Eingriff.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 MIGRATIONS-PRINZIP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Logik aus PlanIt/js/admin.js (~7100 Zeilen) wird aufgeteilt in die 12
Admin-Module. Logik aus PlanIt/js/employee.js (~2400 Zeilen) wird aufgeteilt
in die 7 Mitarbeiter-Module. Funktionen die in beiden PlanIt-Dateien identisch
vorhanden waren landen in js/shared/utils.js. Keine inhaltlichen Änderungen
an der Logik während der Migration — erst migrieren, dann verbessern.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PREISGESTALTUNG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Modular buchbar (web/pricing.html). Alle Preise Netto zzgl. MwSt.
Einmalige Setup-Gebühr: 99 € (Einrichtung, Konfiguration, Einführung)

 KERN-FEATURES
  Schichtplanung              10 € / Monat
  Mitarbeiter-Verwaltung       8 € / Monat
  Urlaubsverwaltung            8 € / Monat
  Zeiterfassung               12 € / Monat

 ABRECHNUNG
  Vorlohnabrechnung           10 € / Monat
  Trinkgeld-Verwaltung         6 € / Monat
  Stundenübersicht             5 € / Monat

 EXTRAS
  Inventur                     6 € / Monat
  Aufgaben & Notizen           4 € / Monat
  Hygiene-Protokoll            4 € / Monat
  Abgänge & Kündigungen        5 € / Monat
  MarginCalc                   5 € / Monat

Beispiel Vollpaket (alle 12 Module): 83 € / Monat + 99 € Setup einmalig.
